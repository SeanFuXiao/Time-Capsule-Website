<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Capsule</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <header>
      <h1>Edit Time Capsule</h1>
      <nav>
        <a href="/dashboard">Dashboard</a>
        <a href="/logout">Logout</a>
      </nav>
    </header>

    <main>
      <form action="/capsules/<%= capsule._id %>/edit" method="POST">
        <div>
          <label for="title">Title:</label>
          <input
            type="text"
            id="title"
            name="title"
            value="<%= capsule.title %>"
            required
          />
        </div>
        <div>
          <label for="content">Content:</label>
          <textarea id="content" name="content" required>
<%= capsule.content %></textarea
          >
        </div>
        <div>
          <label for="unlockDate">Unlock Date:</label>
          <input
            type="date"
            id="unlockDate"
            name="unlockDate"
            value="<%= capsule.unlockDate.toISOString().split('T')[0] %>"
            required
          />
        </div>
       
        <button type="submit">Save Changes</button>
      </form>
    </main>

    <footer>
      <p>&copy; 2024 Digital Time Capsule</p>
    </footer>
  </body>
</html>
